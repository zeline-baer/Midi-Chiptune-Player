# Tested together on Windows x64 with CPython 3.12.10 (2026-09-13).
# Update in a virtual environment and run the tests before changing these pins.
numpy==2.5.3
wxPython==4.3.1
mido==1.3.3
python-rtmidi==1.5.8
sounddevice==0.5.6
numba==0.67.0

# Transitive dependencies, pinned to reproduce the tested environment.
cffi==2.1.1
llvmlite==0.49.0
packaging==26.3
pycparser==3.0
